{% block ezcolor_field %}
    {% spaceless %}
        {% set field_value = field.value.text %}
        {% if parameters.style is defined %}
            style="{{ parameters.style }}: {{ field_value }}"
        {% else %}
            {{ field_value }}
        {% endif %}
    {% endspaceless %}
{% endblock %}
